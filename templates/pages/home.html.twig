<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {% block stylesheets %}
      {{ encore_entry_link_tags('fonts') }}
      {{ encore_entry_link_tags('app') }}
    {% endblock %}
    <link rel="icon" type="image/png" href="/images/skyrim.png" />
    <title>
      {% block title %}
        SkyrimWiki
      {% endblock %}
    </title>
  </head>

  <body>
    <header class="header sticky top-0 bg-neutral-800 text-white z-50">
      <div class="flex justify-between container mx-auto py-3">
        <nav class="flex">
          <a href="/" class="mr-10 font-balgruf flex items-center"><img src="/images/skyrim.png" alt="SkyrimWiki" width="20px" /><span>SkyrimWiki</span></a>
          <ul class="flex">
            <li>
              <a href="/races" class="mr-5">Races</a>
            </li>
            <li>
              <a href="/provinces" class="mr-5">Provinces</a>
            </li>
            <li>
              <a href="/skills" class="mr-5">Skills</a>
            </li>
            <li>
              <a href="/cities" class="mr-5">Cities</a>
            </li>
            <li>
              <a href="/followers" class="mr-5">Followers</a>
            </li>
          </ul>
        </nav>
        <div class="flex">
          {% if app.user %}
            <a class="ml-2" href="/user/{{ app.user.id }}">{{ app.user.username }}</a>
            <a class="ml-2" href="/logout" class=""><img class="h-6 w-6" src="/images/svg/logout.svg" alt="" /></a>
          {% else %}
            <a class="ml-2" href="/login" class="whitespace-nowrap text-base font-medium text-gray-500 hover:text-gray-900">Sign in</a>
            <a class="ml-2" href="/signup" class="ml-8 inline-flex items-center justify-center whitespace-nowrap rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-base font-medium text-white shadow-sm hover:bg-indigo-700">Sign up</a>
          {% endif %}
        </div>
      </div>
    </header>

    <main class="main">
      <img src="/images/home-background.jpg" alt="Skyrim" class="absolute inset-0 object-cover min-h-screen min-w-full" />
      {% block content %}
        <div class="content container mx-auto py-12 z-10 relative">
          <section class="section home-header">
            <h1 class="text-5xl text-center text-white z-10 relative mx-auto font-balgruf">Skyrim <br />Wiki</h1>
            <div class="flex relative z-20 mx-auto">
              <div class="m-2 border border-gray-600 rounded-md py-1 px-2 bg-gray-700/[.6]">
                <a href="/races" class="text-2xl font-balgruf">Races</a>
                <div>
                  <h2 class="text-xl text-white font-balgruff">Latest articles</h2>
                  <ul>
                  {% for race in last_races_articles %}
                    <li><a href="/races/{{ race.name }}">- {{ race.name }}</a></li>
                  {% endfor %}
                  </ul>
                </div>
                <div></div>
              </div>
              <div class="m-2 border border-gray-600 rounded-md py-1 px-2 bg-gray-700/[.6]">
                <a href="/provinces" class="text-2xl font-balgruf">Provinces</a>
                <div>
                  <h2 class="text-xl text-white font-balgruff">Latest articles</h2>
                  <ul>
                  {% for province in last_provinces_articles %}
                    <li><a href="/provinces/{{ province.name }}">- {{ province.name }}</a></li>
                  {% endfor %}
                  </ul>
                </div>
              </div>
              <div class="m-2 border border-gray-600 rounded-md py-1 px-2 bg-gray-700/[.6]">
                <a href="/skills" class="text-2xl font-balgruf">Skills</a>
                <div>
                  <h2 class="text-xl text-white font-balgruff">Latest articles</h2>
                  <ul>
                  {% for skill in last_skills_articles %}
                    <li><a href="/skills/{{ skill.name }}">- {{ skill.name }}</a></li>
                  {% endfor %}
                  </ul>
                </div>
              </div>
              <div class="m-2 border border-gray-600 rounded-md py-1 px-2 bg-gray-700/[.6]">
                <a href="/cities" class="text-2xl font-balgruf">Cities</a>
                <div>
                  <h2 class="text-xl text-white font-balgruff">Latest articles</h2>
                  <ul>
                  {% for city in last_cities_articles %}
                    <li><a href="/cities/{{ city.name }}">- {{ city.name }}</a></li>
                  {% endfor %}
                  </ul>
                </div>
              </div>
              <div class="m-2 border border-gray-600 rounded-md py-1 px-2 bg-gray-700/[.6]">
                <a href="/followers" class="text-2xl font-balgruf">Followers</a>
                <div>
                  <h2 class="text-xl text-white font-balgruff">Latest articles</h2>
                  <ul>
                  {% for follower in last_followers_articles %}
                    <li><a href="/followers/{{ follower.name }}">- {{ follower.name }}</a></li>
                  {% endfor %}
                  </ul>
                </div>
              </div>
            </div>
            <div></div>
          </section>
        </div>
      {% endblock %}
    </main>

    <footer class="footer bg-neutral-800 text-white">
      {% block footer %}
        <div class="flex container mx-auto py-12">
          <p class="text-center text-muted">© 2022 SkyrimWiki, Luca Ciampi</p>
        </div>
      {% endblock %}
    </footer>
  </body>
</html>
