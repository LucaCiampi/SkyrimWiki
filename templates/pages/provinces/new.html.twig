{% extends 'base.html.twig' %}

{% block title %}
  New province
{% endblock %}

{% block content %}
    <a href="/provinces">Go back</a>
    new province
    {{ form_start(province_form) }}
      {# {{ form_row(province_form._token) }} #}
      <form>
          <div class="my-custom-class-for-errors">
              {{ form_errors(province_form) }}
          </div>
          <div class="form-group mb-2">
              <label for="province-description">Name</label>
              {{ form_widget(province_form.name, {'attr': {'class': 'form-control', 'id' : 'province-name'}}) }}
          </div>
          <div class="form-group mb-2">
              <label for="province-description">Description</label>
              {{ form_widget(province_form.description, {'attr': {'class': 'form-control', 'row': '20', 'id' : 'province-description'}}) }}
          </div>
          <div class="form-group mb-2">
              <label for="province-image">Image</label>
              {{ form_widget(province_form.image, {'attr': {'class': 'form-control', 'id' : 'province-image', 'placeholder' : 'https://....'}}) }}
          </div>
          {{ form_row(province_form.submit) }}
      </form>
      {{ form_end(province_form) }}
{% endblock %}